# -*- coding: utf-8 -*-
"""제닉(123330) 트래커 설정."""
from pathlib import Path

BASE = Path(__file__).resolve().parent
DATA = BASE / "data"
DATA.mkdir(exist_ok=True)
SITE = BASE / "site"
SITE.mkdir(exist_ok=True)

EXPORTS_CSV = DATA / "exports.csv"     # 관세청 HS 3307.90 월별 수출 (마스크팩 포함)
PRICE_CSV = DATA / "price.csv"         # 주가 스냅샷 (일별)

CODE = "123330"
UA = "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"
TIMEOUT = 30

# ── 관세청 HS 코드 ────────────────────────────────────────────────
# API의 hsSgn은 6자리까지만 먹으므로 330790으로 조회하고 10자리로 갈라낸다.
# (soil-tracker/customs.py에서 확인한 제약과 같다)
HS6 = "330790"
HS_ITEMS = {
    "3307904000": "mask",      # 마스크 팩  ← 제닉의 직접 대응 품목
    "3307909000": "other",     # 기타
    "3307903000": "lens",      # 콘택트렌즈액
    "3307901000": "depil",     # 제모제
}

# 리포트 대조용 주의사항:
#   유안타 '1Q26 수출 2.49억달러'는 HS 330790 **전체** 합계다.
#   순수 마스크팩(3307904000)만 보면 1.58억달러로 크게 다르다.
#   대시보드는 둘 다 보여주되 헤드라인은 마스크팩 단품을 쓴다.

# ── 제닉 CAPA (유안타 2026-05-14, 교보 2026-06-24) ────────────────
# 연말 기준 생산 캐파(만장)와 가동률(%).
CAPA = [
    (2023, 5928, 18.4),
    (2024, 5988, 74.0),
    (2025, 9731, 78.6),
]
# 교보 탐방노트: 1Q26 8호기 → 4월 10호기 → 5월 11호기 (1Q 대비 CAPA +38%),
# 7월 1호기 추가 증설 예정, 3호기 추가 계획. 1호기당 월 100~120만장.
LINES = [("1Q26", 8), ("2026-04", 10), ("2026-05", 11), ("2026-07E", 12)]
LINE_CAPACITY = (100, 120)   # 호기당 월 생산 만장

# ── 아마존 Best Sellers ──────────────────────────────────────────
# 교보 탐방노트가 짚은 지표: "최대 고객사가 아마존 뷰티 TOP 10 내 꾸준히 랭크,
# 마스크팩 카테고리 1위, 미국 세포라 온라인 스킨케어 베스트셀러 1위".
#
# 노드 선택에 시행착오가 있었다. 계층은
#   11060451 피부 관리 > 11060711 페이스 > 11062031 트리트먼트 및 마스크
#   > 11061121 마스크
# 처음엔 11062031을 썼는데 세럼·여드름 패치가 절반 넘게 섞였다(마스크는 20개 중
# 3개뿐). 하위에 마스크 전용 노드가 따로 있는 걸 뒤늦게 발견해 2026-07-19 교체했다.
# 11061121에서는 20개 중 17개가 실제 마스크고 BIODANCE가 #1이다.
AMAZON_NODE = "11061121"

# 과거에 썼던 노드. CSV에 스냅샷이 남아 있지만 대시보드는 현재 노드만 그린다.
# 두 노드는 순위 체계가 달라 이어 붙이면 안 된다.
AMAZON_NODE_LEGACY = ["11062031"]
AMAZON_CATEGORY = "페이셜 마스크"
AMAZON_URL = "https://www.amazon.com/gp/bestsellers/beauty/" + AMAZON_NODE

# 순위를 개별 추적할 ASIN.
# B0B2RM68G2 = BIODANCE 바이오 콜라겐 리얼 딥 마스크 (하이드로겔 오버나이트).
#   2026-07-19 기준 이 카테고리 #2, 스킨케어 전체 #5.
#   ※ 제닉이 이 제품을 만드는지는 **확인되지 않았다.** 하나 리포트는 최대
#     고객사를 'H사'로만 표기한다. 하이드로겔 오버나이트 마스크라는 제품
#     성격이 제닉 주력과 일치해 후보로 올려둔 것이다. 확인 전까지 근거로 쓰지 말 것.
WATCH_ASINS = ["B0B2RM68G2"]

# 이 키워드가 제목에 있으면 마스크류로 보고 순위 추이를 함께 그린다.
MASK_KEYWORDS = ["마스크", "Mask", "mask"]

# ── 제닉 분기 매출 (억원) ────────────────────────────────────────
# 교보증권 2026-06-24 도표1 (십억원 -> 억원). 네이버는 최근 6분기만 주는데
# 검색량·수출과 겹쳐 보려면 1Q24까지 필요해 고정해 둔다. 확정 실적이다.
QUARTERLY_REVENUE = {
    (2024, 1): 64, (2024, 2): 90, (2024, 3): 151, (2024, 4): 194,
    (2025, 1): 178, (2025, 2): 246, (2025, 3): 236, (2025, 4): 122,
    (2026, 1): 294,
}
# 동행성 차트의 지수 기준 분기. 세 계열 모두 값이 있는 첫 분기다.
INDEX_BASE = (2024, 1)
